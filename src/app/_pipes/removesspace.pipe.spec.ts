import { RemovesspacePipe } from './removesspace.pipe';

describe('RemovesspacePipe', () => {
  it('create an instance', () => {
    const pipe = new RemovesspacePipe();
    expect(pipe).toBeTruthy();
  });
});
